#include <stdio.h>
#include <string.h>

int main()
{
	char texto[25];
	int i, cont;
	
	fgets(texto, 25, stdin);
	
	cont=0;
	
	for(i=0; texto[i]!='\0'; i++)
	{
		if(texto[i]==' ')
		{
			cont++;
		}
	}
	cont+=1;
	
	printf("O texto apresentado contem %d palavras", cont);
	
}
